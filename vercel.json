{
    "version": 2,
    "functions": {
        "api/main.py": { "maxDuration": 10 }
    },
    "builds": [
        {
            "src": "/api/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/api/main" }]
}